import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-geist-sans",
  display: "swap",
});

export const metadata: Metadata = {
  title: "RiskWise AI — Early Academic Risk Detection Platform",
  description:
    "Proactively detect academic risk factors in students and enable timely intervention by mentors, teachers, and coordinators.",
  keywords: ["academic risk", "student intervention", "education dashboard", "attendance tracking"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} antialiased`}>{children}</body>
    </html>
  );
}
